<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Needed to change stream volumes and the audio mode when separating the processed
         output from the source app's own playback. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Without this, setStreamVolume throws while Do Not Disturb is on, and the mute the
         separation strategy depends on silently never happens. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"
        tools:targetApi="34" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />

    <application>
        <!--
          The service must already be in the foreground before MediaProjection is taken on
          Android 14+, so it is started first and only then hands its result Intent to the
          projection manager.
        -->
        <service
            android:name="mmm.source.capture.SystemAudioCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />
    </application>
</manifest>
